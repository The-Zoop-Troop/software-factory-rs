--- crates/domain/src/task.rs
+++ delete field state from struct Task expression in Task::apply
@@ -326,20 +326,17 @@
                     },
                     effects: vec![Effect::AppendNote { task: id, note }],
                 })
             }
 
             // ---- in_verify ------------------------------------------------------------
             (TaskState::InVerify { branch, head }, Event::VerifyPassed) => Ok(Transition {
                 task: Task {
-                    state: TaskState::Mergeable {
-                        branch: branch.clone(),
-                        head: head.clone(),
-                    },
+                     /* ~ changed by cargo-mutants ~ */
                     ..self
                 },
                 effects: vec![Effect::OpenMergeBead {
                     task: id,
                     branch,
                     head,
                 }],
             }),
