--- crates/domain/src/budget.rs
+++ delete field tokens from struct Self expression in Usage::add_tokens
@@ -32,17 +32,17 @@
     pub wall_clock: Duration,
     pub attempts: u32,
 }
 
 impl Usage {
     #[must_use]
     pub fn add_tokens(self, n: u64) -> Self {
         Self {
-            tokens: self.tokens.saturating_add(n),
+             /* ~ changed by cargo-mutants ~ */
             ..self
         }
     }
 
     #[must_use]
     pub fn add_wall_clock(self, d: Duration) -> Self {
         Self {
             wall_clock: Duration::from_seconds(
