--- crates/domain/src/lease.rs
+++ replace Lease::grant -> Self with Default::default()
@@ -10,21 +10,17 @@
     pub holder: AgentId,
     pub claimed_at: Timestamp,
     pub expires: Timestamp,
 }
 
 impl Lease {
     #[must_use]
     pub fn grant(holder: AgentId, now: Timestamp, ttl: Duration) -> Self {
-        Self {
-            holder,
-            claimed_at: now,
-            expires: now + ttl,
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     #[must_use]
     pub fn is_expired(&self, now: Timestamp) -> bool {
         now >= self.expires
     }
 
     /// Extend the lease from `now` (not from the old expiry) so a stalled heartbeat
